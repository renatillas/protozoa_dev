syntax = "proto3";

// Test file for new well-known types
package test;

import "google/protobuf/any.proto";
import "google/protobuf/timestamp.proto";

// Message that references well-known types
message MetadataService {
  string name = 1;
  
  // Force generation by using these types
  google.protobuf.Any config = 2;
  google.protobuf.Timestamp created_at = 3;
  
  // Test message that would benefit from metadata types
  string description = 4;
}